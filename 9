#include<iostream>
using namespace std;
int main()
{
  int a, b, power1=1, sum1=0, power2=1, sum2=0,x;
  cin>>a>>b;
  for(int i=a;i<=b;i++)
  {
    x=i;
    for(int br=0;br<3;br++)
    {
      sum1=sum1+x%10;
      power1=power1*(x%10);
      x=x/10;
      sum2=sum2+x%10;
      power2=power2*(x%10);
      x=x/10;
     }
     
     if(sum1==sum2&&power1==power2) cout<<i<<" ";
     power1=1;
     sum1=0;
     power2=1;
     sum2=0;
  }
}
